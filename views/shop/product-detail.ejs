<%- include('../includes/head.ejs') %>
</head>
<body>
    <%- include('../includes/navigation.ejs') %>
    <div class="container mt-4">
        <div class="row">
            <div class="col-md-6 offset-md-3 product-detail text-center">
                <h1 class="mb-4 text-dark"><%= product.title %></h1>
                <img src="<%= product.imageUrl %>" class="img-fluid rounded mb-3" alt="<%= product.title %>">
                <h2 class="text-primary">$ <%= product.price %></h2>
                <p class="lead text-secondary"><%= product.description %></p>
                <%- include('../includes/add-to-cart.ejs') %>
            </div>
        </div>
    </div>